"""
Utility package for models, helpers, parameters, and VMD components.

This file marks the directory as a Python package for reliable absolute imports
like `from utils.helpers import ...` when running from the project root.
"""

# It's fine to keep this empty; explicit re-exports are not required.
